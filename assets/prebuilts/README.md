# Canonical prebuilt APKs for Scalable UI demos
#
# Use this directory with: ./scripts/deploy_ui.sh
# Mirror (docs legacy path): ../prebuilt_apks/  — keep in sync via sync_prebuilts.sh
#
# Tip demo (default): MultiPanelLandscapeRRO + CarLauncherMultiPanelRRO + MockMap + CarSystemUI
# OEM package demo:   OemDemoRRO (tip panels, prebuilt-safe)
# Pleos chrome:       requires CarSystemUI rebuilt with CarSysuiScalableBarControllers
